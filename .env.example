# 本地开发环境变量示例（Render 上由平台自动注入，无需此文件）
# 复制为 .env 后按需修改；本项目通过 `node --env-file=.env src/index.js` 或手动 export 加载
PORT=3000
# 本地 PostgreSQL 连接串示例
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/douyin_toolbox
